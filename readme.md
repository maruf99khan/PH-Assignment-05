1.var is too old to use and has some issues, let is for changable variable, and const is for constant variable.
2.spread operator spreads only the contents and makes a new data, not array or objects 
3.map makes a new array with some modifications to each element, filter filters the elements and keep only the required ones, forEach is a function that runs with every element of the array/object etc.
4.arrow function is a simpler way of writing a function.
5.template literal is basically a string but it can also include js codes inside it.
